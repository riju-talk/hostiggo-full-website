# Supabase handles Google OAuth — configure it in your Supabase dashboard (Authentication > Providers > Google).
# The app uses NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY from src/lib/supabase.ts
